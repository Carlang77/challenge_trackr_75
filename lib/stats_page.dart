import 'package:flutter/material.dart';

class StatsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Placeholder for stats content
    return Center(
      child: Text('Stats Summary'),
    );
  }
}
